import 'package:flutter/material.dart';

class JailBrokenDialog extends StatelessWidget {
  const JailBrokenDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      child: Center(
        child: Text('你越獄摟!!'),
      ),
    );
  }
}
